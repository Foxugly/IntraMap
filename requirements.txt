# Runtime dependencies (mirror of [project.dependencies] in pyproject.toml)
python-nmap>=0.7.1
PyYAML>=6.0
psutil>=5.9

# Interface graphique (extra "gui") — installer avec : pip install -e .[gui]
# ou directement : pip install PySide6
PySide6>=6.6
